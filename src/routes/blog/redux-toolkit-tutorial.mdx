import { ArticleHead, Button } from '../../components'
export const title = 'Redux Toolkit For State Management'
export const date = 'May 5, 2025'
export const description = "Let's learn about Redux Tookit for state management"
export const thumbnailUrl = '/redux-toolkit-thumbnail.jpeg'

<ArticleHead title={title} date={date} description={description} thumbnailUrl={thumbnailUrl} />

# {title}

<div>
  This is a blog post about <strong>Redux Toolkit</strong>. <br />
  We'll learn about Redux Toolkit for state management.
</div>

<br />

### This is a Markdown sub-heading

We just used a `###` subheading in our blog post.

```
  // this is a code block
  const doSomething = () => {
    return console.log('hey')
  }
```

<div>Here, I'm using a component inside my MarkDown file: {<Button text={'Click me'} />}</div>
