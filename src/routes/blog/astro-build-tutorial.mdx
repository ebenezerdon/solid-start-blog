import { ArticleHead, Button } from '../../components'
export const title = 'Build A Blog With Astro'
export const date = 'November 14, 2022'
export const description = 'Learn Astro by building a blog'
export const thumbnailUrl = '/astro-blog-thumbnail.jpeg'

<ArticleHead title={title} date={date} description={description} thumbnailUrl={thumbnailUrl} />

# {title}

<div>
  This is a blog post about <strong>astro build</strong>. <br />
  We'll learn how to build a blog with Astro and why it's better than the alternatives.
</div>

<br />

### This is a Markdown sub-heading

We just used a `###` subheading in our blog post.

```
  // this is a code block
  const doSomething = () => {
    return console.log('hey')
  }
```

<div>Here, I'm using a component inside my MarkDown file: {<Button text={'Click me'} />}</div>
