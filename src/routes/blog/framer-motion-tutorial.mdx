import { ArticleHead, Button } from '../../components'
export const title = 'Framer Motion: The React Motion Library'
export const date = 'November 17, 2023'
export const description = "Here's an introduction to Framer Motion."
export const thumbnailUrl = '/framer-motion-thumbnail.webp'

<ArticleHead title={title} date={date} description={description} thumbnailUrl={thumbnailUrl} />

# {title}

<img src={thumbnailUrl} alt={title} />

<div>
  This is a blog post about <strong>Framer Motion</strong>. <br />
  We'll learn how to use Framer motion to animate React apps.
</div>

<br />

### This is a Markdown sub-heading

We just used a `###` subheading in our blog post.

```
  // this is a code block
  const doSomething = () => {
    return console.log('hey')
  }
```

<div>Here, I'm using a component inside my MarkDown file: {<Button text={'Click me'} />}</div>
